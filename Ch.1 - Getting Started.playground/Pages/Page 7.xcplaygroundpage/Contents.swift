//: [Previous](@previous)

/*: 
# REPL

* READ
* EVAL
* PRINT
* LOOP

Open the terminal and type `swift` and opens the REPL.
To exit out of repltype `:q`. You can hit `tab` to do auto-completion or go up and down for history.


You can also use REPL during a debugging session of Xcode.
Type in `repl` in the debugging console. REPL's auto-compleition and session history is not available during Xcode debugging sessions.

*/

//: [Next](@next)
